# Mora 设计语言

> 版本：3.0 · 更新日期：2026-08-05 · 状态：产品 UI 中文设计基准
>
> 品牌主张：**让知识彼此连接、持续有用、保持生命力。**

本文定义 Mora 在 `web/` 中的视觉语言、交互行为和工程约定，设计基础来自 `design-assets/brand/mora-master.svg`。英文基准为 [`design.md`](./design.md)。已归档的 v2 文档仅作历史参考。

---

## 1. 品牌基础

Mora 是一个私有化部署的协同知识工作台。品牌标志由三个相连节点构成：独立知识单元在关系可见时产生更大价值，其中一个绿色节点代表正在被创建、讨论或应用的活跃知识。

### Logo 含义

| 元素 | 含义 | 产品启示 |
|---|---|---|
| 三个节点 | 文档、人和 Agent 等独立知识参与者 | 保留身份和来源 |
| 三角连接 | 关系把孤立内容变成知识系统 | 让上下文和链接可见 |
| 墨黑节点 | 持久、可信的知识 | 中性表面承载大部分信息 |
| 绿色节点 | 活跃贡献和有效变化 | 绿色标记意图、进度和焦点 |
| 圆角字标 | 具有人性节奏的技术产品 | 精确但不机械 |

Logo 是品牌资产，不是组件库。不要把卡片旋转成菱形，不要用节点三角形装饰页面，也不要把所有关系画成可见连线。

### 设计性格

- **连接**：关系和上下文可见，但界面不应变成网络图。
- **编辑感**：阅读和写作品质优先于仪表盘装饰。
- **沉静**：稳定几何、安静表面和克制动效帮助专注。
- **精确**：状态、归属、权限和操作后果始终明确。
- **有生命力**：活跃工作获得及时绿色反馈，非活跃界面退居背景。

### 产品原则

1. **内容主导**：文档是主界面，工具为内容服务。
2. **上下文跟随内容**：位置、所有者、权限、保存状态和关系始终可发现。
3. **绿色表达活跃意图**：品牌绿只用于操作、焦点、进度和有意义的选中。
4. **先结构后装饰**：先用对齐、间距和边框，再使用卡片、颜色和阴影。
5. **协作可追溯**：成员与 AI 产出始终保留身份和来源。
6. **沉静不等于空旷**：布局紧凑有序，不靠巨大留白制造高级感。

### 明确不做

- 不使用蓝紫科技主题。
- 不使用全绿界面；绿色必须保持稀缺才能传达含义。
- 不做卡片优先的后台仪表盘。
- 不把网络图语言套用到所有页面。
- 不做 AI 对话壳或插画主导的营销界面。

---

## 2. 品牌资产

### 品牌母版

- 源文件：`design-assets/brand/mora-master.svg`
- 生产字标使用轮廓路径，不依赖运行时字体。
- 产品图标和 favicon 必须从节点标志派生，不手工重画。

### 安全距离和尺寸

- 四周安全距离至少为单个节点宽度的一半。
- 完整组合标志最小宽度 `120px`。
- 独立图标最小尺寸 `20px`；低于 `20px` 使用光学校正的简化 favicon。
- 保持原始宽高比和路径几何。

### 颜色版本

| 版本 | 标志 | 字标 | 表面 |
|---|---|---|---|
| 主版本 | 墨黑 + Mora Green | 墨黑 | 浅色中性背景 |
| 反白版 | 柔白 + Active Green | 柔白 | 深色中性背景 |
| 深色单色版 | 墨黑 | 墨黑 | 浅色或透明背景 |
| 浅色单色版 | 柔白 | 柔白 | 深色或图片背景 |

禁止添加渐变、阴影、立体效果、描边或额外节点色。主版本不得放在绿色背景上，否则活跃节点会失去含义。

---

## 3. 色彩系统

### 3.1 Logo 颜色

以下值是不可变的品牌识别锚点：

| 令牌 | 值 | 角色 |
|---|---|---|
| `--brand-ink` | `#202523` | 字标和持久知识节点 |
| `--brand-line` | `#252A28` | 关系和结构连接 |
| `--brand-green` | `#12A77C` | 活跃知识节点 |

`--brand-green` 针对 Logo 识别优化，但不适合所有小文字或白字按钮。产品组件使用下方满足可访问性的交互令牌。

### 3.2 绿色阶

| 令牌 | 值 | 用途 |
|---|---|---|
| `--green-50` | `#ECF9F5` | 轻微页面高亮 |
| `--green-100` | `#D7F3EA` | 选中背景 |
| `--green-200` | `#A9E5D4` | 进度轨道、装饰数据 |
| `--green-300` | `#6DD0B4` | 深色主题次级强调 |
| `--green-400` | `#35BB96` | 深色主题悬停 |
| `--green-500` | `#12A77C` | Logo 和大面积图形强调 |
| `--green-600` | `#0E8967` | 中等强调 |
| `--green-700` | `#0B7659` | 浅色主题可访问主色 |
| `--green-800` | `#095F49` | 悬停和强调文字 |
| `--green-900` | `#074C3B` | 按下状态 |

### 3.3 浅色主题

| 语义令牌 | 值 | 角色 |
|---|---|---|
| `--background` | `#F7F9F8` | 应用画布 |
| `--foreground` | `#202523` | 主文字 |
| `--surface` / `--card` | `#FFFFFF` | 编辑器、气泡层、弹窗 |
| `--surface-subtle` | `#EFF3F1` | 侧栏和组合控件 |
| `--muted-foreground` | `#626B67` | 次要文字 |
| `--border` | `#DCE2DF` | 结构分隔 |
| `--primary` | `#0B7659` | 主操作和可访问链接 |
| `--primary-hover` | `#095F49` | 悬停 |
| `--primary-active` | `#074C3B` | 按下 |
| `--primary-foreground` | `#FFFFFF` | 主色上的文字 |
| `--accent` | `#D7F3EA` | 选中和悬停表面 |
| `--accent-foreground` | `#07533F` | 强调表面上的文字 |
| `--ring` | `#0B7659` | 键盘焦点 |

### 3.4 深色主题

| 语义令牌 | 值 | 角色 |
|---|---|---|
| `--background` | `#121614` | 应用画布 |
| `--foreground` | `#F3F6F4` | 主文字 |
| `--surface` / `--card` | `#191E1C` | 编辑器、气泡层、弹窗 |
| `--surface-subtle` | `#202624` | 侧栏和组合控件 |
| `--muted-foreground` | `#AAB3AF` | 次要文字 |
| `--border` | `#343C38` | 结构分隔 |
| `--primary` | `#45D1A8` | 主操作和链接 |
| `--primary-hover` | `#66DCB9` | 悬停 |
| `--primary-active` | `#2BBE94` | 按下 |
| `--primary-foreground` | `#10221C` | 主色上的文字 |
| `--accent` | `#173D32` | 选中和悬停表面 |
| `--accent-foreground` | `#80E3C7` | 强调表面上的文字 |
| `--ring` | `#45D1A8` | 键盘焦点 |

深色模式独立设计，不通过机械反转生成。大面积深色使用温和墨黑中性色，不使用蓝灰色。

### 3.5 语义色与功能色

| 含义 | 浅色 | 深色 | 用途 |
|---|---|---|---|
| 成功 | `#0E7A52` | `#56D39A` | 已保存、在线、已完成 |
| 警告 | `#9A6209` | `#F0BC62` | 只读、降级、需要关注 |
| 危险 | `#B83B4A` | `#FF7D8A` | 错误、删除、拒绝访问 |
| 信息 | `#3D6872` | `#78C0CE` | 同步、索引、中性信息 |

状态不得只依赖颜色，必须同时使用文字、图标、形状或位置。协作光标可使用更宽的功能色板，但必须同时显示成员姓名。

### 3.6 用色纪律

- 普通产品表面保持中性，不使用大面积绿色背景。
- 每个操作区域只出现一个主绿色操作。
- 当绿色选中状态可能与成功状态冲突时，必须用标签和图标区分。
- AI 使用墨黑 + 绿色、Sparkles 图标和来源标签，不再拥有独立紫色品牌。
- 工作台禁止渐变、绿色发光和装饰性网络连线。

---

## 4. 字体

使用本地和系统字体，保证私有化部署不依赖外部字体请求。

```css
--font-sans: Inter, "HarmonyOS Sans", "Source Han Sans SC", "Microsoft YaHei", system-ui, sans-serif;
--font-mono: "JetBrains Mono", "SFMono-Regular", Consolas, ui-monospace, monospace;
```

| 角色 | 字号 / 行高 | 字重 | 用途 |
|---|---|---|---|
| 文档标题 | `32 / 40px` | 600 | 仅用于可编辑文档标题 |
| 页面标题 | `20 / 28px` | 600 | 搜索、设置、历史 |
| 分区标题 | `16 / 24px` | 600 | 面板分区 |
| 正文 | `15 / 24px` | 400 | 产品文案 |
| 编辑器正文 | `16 / 28px` | 400 | 长文内容 |
| UI 标签 | `14 / 20px` | 500 | 控件和导航 |
| 元数据 | `12 / 18px` | 400–500 | 时间、状态、帮助文字 |
| 代码 | `13 / 21px` | 400 | 代码和技术标识符 |

- 产品 UI 字间距统一为 `0`；定制字距仅用于已轮廓化的品牌资产。
- 字号不随视口宽度缩放。
- 除 MCP、RBAC 等必要缩写外，避免全大写。
- 编辑器段落每行推荐 `60–75` 个拉丁字符。

---

## 5. 间距、形状与层级

### 间距

采用 `4px` 基础单位：`4, 8, 12, 16, 24, 32, 48`。

- 紧凑控件 `32px`，默认控件 `36px`，突出操作 `40px`。
- 桌面图标点击区至少 `36 × 36px`，触控目标 `44 × 44px`。
- 侧栏行高 `32px`，面板标题栏 `44px`，文档工具栏 `44–48px`。
- 正文宽度 `680–760px`，表格和媒体可使用可用内容宽度。
- 主内容边距：桌面 `24px`，平板 `16px`，手机 `12px`。

### 形状

| 令牌 | 值 | 用途 |
|---|---|---|
| `--radius-sm` | `4px` | 目录行、标签、紧凑控件 |
| `--radius-md` | `6px` | 按钮、输入框、菜单 |
| `--radius-lg` | `8px` | 弹窗、气泡层、重复对象卡片 |

- 产品 UI 圆角不超过 `8px`。
- Logo 的旋转节点不代表 UI 容器也应旋转。
- 胶囊形仅用于成员、标签和状态。

### 边框与阴影

- 先使用 `1px` 边框建立结构，再考虑阴影。
- 侧栏、工具栏、编辑器区域和页面分区不是卡片。
- 气泡层使用轻阴影，弹窗使用较强阴影和遮罩。
- 普通行内内容和导航不使用阴影。
- 禁止卡片嵌套。

---

## 6. 应用布局

### 工作台外壳

```text
┌──────────────────┬──────────────────────────────────────────┐
│ 工作区           │ 文档：标题 · 状态 · 成员 · 工具         │
│ 侧栏 240–288px   ├──────────────────────────────────────────┤
│                  │ 上下文工具栏                             │
│ 目录 / 搜索 /    ├──────────────────────────────────────────┤
│ 权限 / 历史      │ 文档画布                                 │
└──────────────────┴──────────────────────────────────────────┘
```

- 侧栏默认宽度 `272px`，标签长度不得改变宽度。
- 正文在文档画布中居中，不使用装饰卡片包裹编辑器。
- 搜索、权限和历史优先作为上下文面板。
- 常驻标题栏只包含页面身份、保存状态、协作者和高频操作。

### 响应式行为

| 宽度 | 行为 |
|---|---|
| `≥ 1200px` | 完整侧栏和文档，可附加右侧上下文面板 |
| `768–1199px` | 侧栏可折叠，同一时间只展示一个上下文面板 |
| `< 768px` | 侧栏变为模态抽屉，文档标题栏压缩为单行 |

任何宽度下都保留文档标题、保存状态和返回导航的路径。文字缩小或重叠前，先把次要命令移入菜单。

### 层级规则

- 主要结构使用全宽区域和分隔线。
- 卡片仅用于搜索结果、模板和集成等离散重复对象。
- 同级视图使用标签页，模式使用分段控件，二元设置使用开关，选项集合使用菜单。
- 每个区域只有一个主操作，次要命令使用 outline 或 ghost。

---

## 7. 核心交互模式

### 导航

- 悬停反馈轻微；选中状态持久，使用 `accent` 并配合文字或主色指示。
- 目录树每级缩进 `16px`。
- 用户长标题单行截断，并可按需查看完整内容。
- 键盘焦点始终可见，且与选中状态不同。

### 编辑与保存

- 直接编辑，不在文档内容外再包一层表面。
- 标题附近显示 `正在保存…`、`已保存` 或 `保存失败`。
- 自动保存成功使用行内状态，不连续弹 Toast。
- 只读状态在输入前显示，并解释原因。
- 重要操作明确对象和恢复后果。

### 搜索与关系

- 进入搜索时聚焦输入框，结果支持键盘导航。
- 命中词使用语义令牌，不使用硬编码黄色。
- 结果显示标题、两行摘要和紧凑元数据。
- 生效中的筛选保持可见并可逐项移除。
- 反向链接和相关页面使用安静列表或图谱摘要，不把装饰性网络图放在内容背后。

### 协作

- 最多直接显示 3 个成员头像，其余使用 `+N`。
- 成员身份色在一次会话中保持稳定。
- `在线`、`正在编辑`、`只读` 和连接降级状态使用文字、图标和颜色共同表达。
- 光标更新不得触发布局动画。

### AI

- AI 入口使用 Sparkles、墨黑/绿色，以及描述结果的名称，如 `总结此页`。
- 生成内容显示来源、待审阅状态和接受/放弃操作。
- AI 不自动覆盖用户内容。
- 流式反馈只出现在受影响区域。
- 不使用紫色光晕、漂浮圆球、粒子场或独立的 AI 视觉宇宙。

### 加载、空、错误与成功

| 状态 | 模式 |
|---|---|
| 加载 | 与最终结构一致的骨架；短暂未知动作才使用 Spinner |
| 空 | 一个简洁图标、标题、有用说明和可选主操作 |
| 错误 | 说明失败内容、数据是否安全，并提供恢复操作 |
| 成功 | 持久状态使用行内确认，已完成命令使用 Toast |
| 禁用 | 降低强调；原因不明显时提供可访问说明 |

功能数据加载时保留应用外壳。空状态不是功能教程。

---

## 8. 组件与图标

### 按钮

- `primary`：每个区域唯一的主要命令。
- `outline` / `secondary`：替代或可逆命令。
- `ghost`：工具栏和低强调命令。
- `destructive`：危险操作确认，不用于每个删除图标。
- 图标按钮尺寸固定，并具有可访问名称。

### 输入控件与浮层

- 标签始终可见，placeholder 提供示例而不是标签。
- 校验信息通过 `aria-describedby` 关联。
- 焦点使用 `ring`，错误使用危险色边框和文字。
- 菜单承载简短动作，气泡层承载轻量上下文，弹窗承载聚焦或重要任务。
- 弹窗实现初始焦点、焦点圈定、Escape 和焦点归还。

### 图标

统一使用 `lucide-react`，描边 `1.75–2px`：紧凑控件 `16px`，标准控件 `18–20px`，空状态 `24px`。熟悉命令使用熟悉符号，不明显的纯图标操作必须提供 Tooltip。

### 表格与 Diff

- 行高 `36–44px`，长表格表头保持可见。
- 数字右对齐，文本左对齐。
- 新增和删除行同时使用 `+` / `−` 与颜色。
- 技术比较优先横向滚动，不拆成卡片。

---

## 9. 动效与无障碍

### 动效

| 交互 | 时长 | 曲线 |
|---|---|---|
| 悬停 / 聚焦 | `120ms` | `ease-out` |
| 菜单 / 气泡层 | `160ms` | `ease-out` |
| 侧栏 / 面板 | `200ms` | `ease-out` |
| 弹窗 | `220ms` | `ease-out` |

动效解释因果和变化。不使用弹跳、视差或持续环境动画。遵循 `prefers-reduced-motion: reduce`。

### 无障碍与本地化

- 满足 WCAG 2.2 AA：普通文字 `4.5:1`，大文字和有意义图形 `3:1`。
- 所有工作流支持键盘，焦点顺序合理且始终可见。
- 状态、作者和权限不得只依赖颜色。
- 优先使用语义 HTML，再使用 ARIA；异步状态需适当播报。
- 文案容器至少容纳当前英文标签长度的 `1.5×`。
- 日期、时间、数字和复数使用本地化格式化 API。

---

## 10. 文案语气

Mora 的表达简洁、温和、可执行。

| 场景 | 推荐 | 避免 |
|---|---|---|
| 保存 | 已保存 | 操作成功 |
| 加载工作区 | 正在准备工作空间… | 正在初始化系统资源… |
| 创建 | 新建页面 / 新建知识库 | 创建新的知识空间 |
| 权限 | 你没有编辑此页面的权限 | 403 Forbidden |
| 搜索为空 | 未找到相关内容 | 无数据 |
| 删除 | 删除页面？此页面可从回收站恢复。 | 确定执行操作？ |

按钮使用清晰动作词。例行反馈不用感叹号。错误说明发生了什么、数据是否安全，以及下一步可以做什么。

---

## 11. 工程约定

### 令牌规则

- 组件使用 `bg-background`、`text-foreground`、`bg-primary`、`text-warning` 等语义类。
- 除经批准的成员身份色和第三方嵌入内容外，组件禁止直接使用 HEX、RGB 和 Tailwind 色板类。
- 新令牌必须同时包含浅色和深色值。
- Logo 常量属于品牌资产，可访问交互令牌属于产品主题。

### 组件归属

- 通用基础组件：`web/src/components/ui/`。
- 领域模式：`editor/`、`search/`、`collab/`、`history/` 和 `rbac/`。
- 优先扩展已有基础组件，不建立视觉重复组件。
- 不依赖生产数据也能展示和测试组件状态。

### 完成定义

1. 在浅色和深色主题中使用语义令牌。
2. 覆盖相关默认、悬停、焦点、激活、禁用、加载、空和错误状态。
3. 支持键盘导航和可访问名称。
4. 在 `320px`、`768px`、`1280px` 和 `200%` 缩放下不溢出。
5. 长标题、空数据和慢响应不会破坏布局。
6. 在 `web/` 中通过 `npm run typecheck`、`npm run lint` 和 `npm run build`。
7. 在桌面、手机宽度和两种主题下完成视觉检查。

### 迁移顺序

1. 将 `web/src/index.css` 中的灰阶和蓝紫变量替换为 §3 的语义令牌。
2. 从 `design-assets/brand/mora-master.svg` 派生 `web/public/logo.svg` 和 favicon。
3. 挂载 Theme Provider，提供浅色 / 深色 / 跟随系统控件。
4. 清理目录、编辑器、协作、搜索和历史中的硬编码颜色。
5. 统一保存、加载、空、错误、Toast 和状态 Badge。
6. 先完善工作台外壳和编辑器排版，再添加视觉细节。

---

## 12. 设计评审清单

- 文档或当前任务是否仍是屏幕上最强的元素？
- 用户能否识别工作区、页面、权限、保存状态和协作者？
- 绿色是否在表达活跃意图，而不是填满空间？
- 是否先使用对齐和边框，再使用卡片和阴影？
- Logo 节点是否仅用于有意义的品牌或关系场景？
- AI 产出和协作者身份是否可追溯？
- 危险操作是否明确并尽可能可恢复？
- 两种主题、键盘操作和手机宽度是否支持完整工作流？
- 真实长文是否保持可读？
- 每个视觉值是否属于语义令牌或明确记录的例外？

