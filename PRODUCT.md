# Mora 产品定义

> 版本：v0.1 ｜ 更新日期：2026-08-14

## 一句话

Mora 是面向人与 AI Agent 的受治理知识底座 —— 让文档、代码、记忆和技能在同一个空间中被发现、治理和交付。

## 产品定位

Mora 是组织知识的**治理与交付控制面**。它不替代已有的文档系统、代码仓库或 Agent 运行时，而是在它们之上建立统一的身份、权限、版本、来源和检索层。

| 角色 | 在 Mora 中做什么 |
|------|-----------------|
| **人类** | 创造、校正和确认知识；编写文档、审阅记忆、管理技能 |
| **Agent** | 发现、应用、提炼和反馈知识；通过 MCP 工具或 Context Proxy 获取上下文 |
| **Mora** | 保存结构、来源、权限、版本和生命周期；确保每一次知识交付都经过授权 |

## 核心能力

### 1. 协作文档

块级 Markdown 编辑，双向可逆转换，往返不丢信息。多工作区隔离，无限级目录树，版本历史不可改写，支持 Diff 与回滚。多人实时协同编辑，基于 Yjs CRDT，并发超限自动降级。

### 2. 权限治理

RBAC 权限引擎，决策优先级「显式拒绝 > 显式允许 > 继承 > 默认拒绝」。权限全链路穿透：API、检索、MCP 调用、Context Proxy 均复用同一决策。存在性不泄露：无权限的文档不出现在目录树和检索结果中。

### 3. 混合检索

BM25 全文检索与向量 RAG 并行，RRF 融合排序。RBAC 为硬约束，在召回阶段即过滤，不依赖后处理。支持多维度筛选，检索结果带来源和版本追溯。

### 4. Agent 知识交付

统一知识资产模型：文档、代码库、记忆、技能四种一级资产类型共享同一套身份、权限和版本体系。

- **MCP Server**：Agent 主动调用工具，搜索文档、读取内容、发现资产。受 RBAC 与速率限制约束。
- **Context Proxy**：可选中间层，在被调用 LLM 时被动注入相关记忆和技能，同时自动捕获会话证据，不依赖 Agent 的自觉性。
- **记忆沉淀**：Agent 会话中产生的经验经「候选 → 去重 → 审阅 → 发布」流程，成为团队可复用的知识。
- **技能管理**：可复用工作流、触发条件和验证规则，版本化管理，支持导入导出，校验通过后交付 Agent。

### 5. 知识生命周期

每条知识都带来源、版本、治理状态、置信度和有效期。自动生成的内容默认进入候选态，不直接污染正式知识。Wiki 页面通过增量维护、依赖追踪和冲突检查保持新鲜。所有结论都可回到原始证据。

### 6. 私有化部署

Docker Compose 一键启动全部服务（API、Worker、MCP Server、PostgreSQL、Qdrant、Valkey、MinIO、TEI）。K8s/Helm 支持生产级多副本部署。核心能力不依赖外部服务；外部连接必须显式配置、审计和限制。

## 技术架构

| 维度 | 选择 |
|------|------|
| 架构风格 | Go 模块化单体（modular monolith），单仓四入口 |
| 进程 | mora-api / rag-worker / knowledge-worker / mcp-server / yjs-server |
| 元数据 | PostgreSQL 16 + pgx v5，含全文检索（zhparser） |
| 向量库 | Qdrant，payload 携带 RBAC 过滤字段 |
| 消息 | Valkey Streams + 事务 Outbox，consumer group 消费 |
| 对象存储 | MinIO / S3 兼容 |
| 协同 | Yjs CRDT + y-websocket |
| 鉴权 | JWT（用户）+ delegated session（Agent） |
| Embedding | TEI（默认）/ Ollama，本地化推理 |
| 可观测 | Prometheus + Grafana + OpenTelemetry |

## 设计原则

1. **知识与索引分离**：索引是可重建的派生物，资产及其来源才是事实记录。
2. **证据先于结论**：记忆和生成内容必须能回到会话、文档、代码或工具执行记录。
3. **候选先于发布**：自动提炼内容默认进入候选态，不直接污染正式知识。
4. **权限先于相关性**：先确定可见范围，再进行召回、排序和上下文组装。
5. **按需优于全量注入**：Agent 通过工具逐步读取，避免上下文噪声和权限扩大。
6. **引擎可替换**：Mora 统一治理，文档解析、CodeGraph、记忆提炼等类型引擎通过稳定接口接入。

## 与同类项目的区别

Mora 不是另一个通用 RAG 框架或 Agent 编排平台。它聚焦于**受治理的知识底座**：

- 对文档系统，Mora 是自带治理和 Agent 交付层的知识库。
- 对 Agent 框架，Mora 是可被 MCP 协议集成、可选 Context Proxy 增强的知识后端。
- 对记忆系统，Mora 强调候选审核、来源追溯和团队发布流程，而非自动全量记录。
- 对代码知识库，Mora 通过 CodeGraph 保留符号与调用关系，不把代码压平为文本 Chunk。

## 非目标

- 不替代通用聊天客户端或 Agent 编排平台。
- 不在第一阶段接管完整 Agent Runtime。
- 不把所有会话记录自动发布为团队知识。
- 不用一个统一向量索引替代代码图、全文索引、版本系统和结构化查询。
- 不绑定单一 CodeGraph、记忆模型或 Agent 框架实现。

## 许可证

Apache 2.0
