# grafana/dashboards/dashboards.yml — 自动加载仪表盘
apiVersion: 1
providers:
  - name: "Wiki Platform"
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards
