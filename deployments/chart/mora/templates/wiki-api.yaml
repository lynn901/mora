---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "mora.fullname" . }}-mora-api
  labels:
    app.kubernetes.io/component: mora-api
    {{- include "mora.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.moraApi.replicaCount }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: mora-api
      {{- include "mora.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app.kubernetes.io/component: mora-api
        {{- include "mora.selectorLabels" . | nindent 8 }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/metrics"
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: mora-api
          image: "{{ include "mora.image" . }}{{ .Values.moraApi.image.repository }}:{{ .Values.moraApi.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8080
              protocol: TCP
          env:
            - name: HTTP_ADDR
              value: ":8080"
            - name: DATABASE_URL
              value: "postgres://{{ .Values.postgresql.auth.username }}:$(POSTGRES_PASSWORD)@{{ include "mora.fullname" . }}-postgresql:5432/{{ .Values.postgresql.auth.database }}?sslmode=disable"
            - name: VALKEY_URL
              value: "{{ include "mora.fullname" . }}-valkey:6379"
            - name: QDRANT_URL
              value: "http://{{ include "mora.fullname" . }}-qdrant:6333"
            - name: TEI_URL
              value: "http://{{ include "mora.fullname" . }}-tei:8080"
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "mora.fullname" . }}-secrets
                  key: jwt-secret
            - name: INTERNAL_SERVICE_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ include "mora.fullname" . }}-secrets
                  key: internal-service-token
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "mora.fullname" . }}-secrets
                  key: postgres-password
          envFrom:
            - configMapRef:
                name: {{ include "mora.fullname" . }}-config
          resources:
            {{- toYaml .Values.moraApi.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 15
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /ready
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "mora.fullname" . }}-mora-api
  labels:
    app.kubernetes.io/component: mora-api
    {{- include "mora.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.moraApi.servicePort }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/component: mora-api
    {{- include "mora.selectorLabels" . | nindent 4 }}
{{- if .Values.moraApi.hpa.enabled }}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "mora.fullname" . }}-mora-api
  labels:
    app.kubernetes.io/component: mora-api
    {{- include "mora.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "mora.fullname" . }}-mora-api
  minReplicas: {{ .Values.moraApi.hpa.minReplicas }}
  maxReplicas: {{ .Values.moraApi.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.moraApi.hpa.targetCPUUtilizationPercentage }}
{{- end }}
